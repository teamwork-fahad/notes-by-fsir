---
const verificationCode = import.meta.env.PUBLIC_ALGOLIA_SITE_VERIFICATION || import.meta.env.ALGOLIA_SITE_VERIFICATION || "";
---

{verificationCode && (
  <meta name="algolia-site-verification" content={verificationCode} />
)}
