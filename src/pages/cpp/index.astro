---
import { getCollection } from "astro:content";
import SubjectIndex from "../../components/SubjectIndex.astro";
import { getSubjectBySlug } from "../../lib/subjects";

const meta = getSubjectBySlug("cpp");
if (!meta || meta.published === false) {
  return Astro.redirect("/");
}
const notes = await getCollection("cpp");
---
<SubjectIndex notes={notes} slug="cpp" label={meta.name} title={meta.name} description={meta.description} />


