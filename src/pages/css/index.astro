---
import { getCollection } from "astro:content";
import SubjectIndex from "../../components/SubjectIndex.astro";
import { getSubjectBySlug } from "../../lib/subjects";

const meta = getSubjectBySlug("css");
if (!meta || meta.published === false) {
  return Astro.redirect("/");
}
const notes = await getCollection("css");
---
<SubjectIndex notes={notes} slug="css" label={meta.name} title={meta.name} description={meta.description} />


