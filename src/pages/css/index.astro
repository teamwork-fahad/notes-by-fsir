---
import { getCollection } from "astro:content";
import SubjectIndex from "../../components/SubjectIndex.astro";
const notes = await getCollection("css");
---
<SubjectIndex notes={notes} slug="css" label="CSS" title="CSS Styling" description="Learn CSS fundamentals, layout, selectors, and practical styling examples." />
