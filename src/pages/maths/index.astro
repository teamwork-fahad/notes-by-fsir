---
import { getCollection } from "astro:content";
import SubjectIndex from "../../components/SubjectIndex.astro";
import { getSubjectBySlug } from "../../lib/subjects";

const meta = getSubjectBySlug("maths");
if (!meta || meta.published === false) {
  return Astro.redirect("/");
}
const notes = await getCollection("maths");
---
<SubjectIndex notes={notes} slug="maths" label={meta.name} title={meta.name} description={meta.description} />


