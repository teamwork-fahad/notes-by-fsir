---
import { getCollection } from "astro:content";
import "../../styles/site.css";

const posts = await getCollection("python");
---

<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Python — Notes By FSir</title>
  </head>
  <body>
    <h1>Python Notes</h1>
    <ul>
      {posts.map((p) => (
        <li><a href={`/python/${p.id}`}>{p.data.title}</a></li>
      ))}
    </ul>
  </body>
</html>
