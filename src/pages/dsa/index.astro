---
// Minimal index page for a content collection. This file lists all notes in the collection.
---

<script context="module">
  import { getCollection } from "astro:content";
  export async function get() {
    const posts = await getCollection("dsa");
    return { props: { posts } };
  }
</script>

<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>DSA — Notes By FSir</title>
  </head>
  <body>
    <h1>DSA Notes</h1>
    <ul>
      {Astro.props.posts.map((p) => (
        <li><a href={`/dsa/${p.id}`}>{p.data.title}</a></li>
      ))}
    </ul>
  </body>
</html>
