---
import { getCollection } from "astro:content";
import SubjectIndex from "../../components/SubjectIndex.astro";
import { getSubjectBySlug } from "../../lib/subjects";

const meta = getSubjectBySlug("dsa");
if (!meta || meta.published === false) {
  return Astro.redirect("/");
}
const notes = await getCollection("dsa");
---
<SubjectIndex notes={notes} slug="dsa" label={meta.name} title={meta.name} description={meta.description} />


