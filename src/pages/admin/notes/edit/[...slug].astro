---
import { getCollection } from "astro:content";

export async function getStaticPaths() {
  const notes = await getCollection("php");
  return notes.map((note) => ({
    params: { slug: note.id },
  }));
}

return Astro.redirect(`/admin/notes/edit/php/${Astro.params.slug}`);
---
