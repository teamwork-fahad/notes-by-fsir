---
import { getCollection } from "astro:content";
import SubjectIndex from "../../components/SubjectIndex.astro";
import { getSubjectBySlug } from "../../lib/subjects";

const meta = getSubjectBySlug("docs");
if (!meta || meta.published === false) {
  return Astro.redirect("/");
}
const notes = await getCollection("docs");
---
<SubjectIndex notes={notes} slug="docs" label={meta.name} title={meta.name} description={meta.description} />


