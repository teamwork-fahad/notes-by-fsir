---
import { getCollection } from "astro:content";
import SubjectIndex from "../../components/SubjectIndex.astro";
const notes = await getCollection("mysql");
---
<SubjectIndex notes={notes} slug="mysql" label="MySQL" title="MySQL & SQL" description="Learn SQL queries, DBMS concepts, exercises, SQLite, and database practice." />
