---
import { getCollection } from "astro:content";
import SubjectIndex from "../../components/SubjectIndex.astro";
import { getSubjectBySlug } from "../../lib/subjects";

const meta = getSubjectBySlug("java");
if (!meta || meta.published === false) {
  return Astro.redirect("/");
}
const notes = await getCollection("java");
---
<SubjectIndex notes={notes} slug="java" label={meta.name} title={meta.name} description={meta.description} />

