---
import { getCollection } from "astro:content";
import SubjectIndex from "../../components/SubjectIndex.astro";
import { getSubjectBySlug } from "../../lib/subjects";

const meta = getSubjectBySlug("php");
if (!meta || meta.published === false) {
  return Astro.redirect("/");
}
const notes = await getCollection("php");
---
<SubjectIndex notes={notes} slug="php" label={meta.name} title={meta.name} description={meta.description} />


