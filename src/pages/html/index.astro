---
import { getCollection } from "astro:content";
import SubjectIndex from "../../components/SubjectIndex.astro";
const notes = await getCollection("html");
---
<SubjectIndex notes={notes} slug="html" label="HTML" title="HTML Development" description="Learn HTML structure, elements, semantics, and practical web page building." />
